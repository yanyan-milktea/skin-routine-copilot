# Server-only secret. Never commit a real value.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash

# Unset, empty, or whitespace-only values use https://skin-routine-ai-api.vercel.app.
# To call this app's local API routes instead, set the exact local origin printed
# by the dev server, for example: NEXT_PUBLIC_AI_API_URL=http://localhost:5173
NEXT_PUBLIC_AI_API_URL=
