# Server-only secret. Never commit a real value.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash

# Keep empty for local development so the browser calls /api/generate-routine.
# The deployed frontend falls back to the existing Vercel production proxy.
NEXT_PUBLIC_AI_API_URL=
